# =============================================================================
# Secret - Sensitive configuration values
# =============================================================================
# NOTE: In production, use a secrets manager (Vault, AWS Secrets Manager, etc.)
# Base64 encoded values. Decode: echo "<value>" | base64 -d
apiVersion: v1
kind: Secret
metadata:
  name: cloudlink-secrets
  namespace: cloudlink
  labels:
    app.kubernetes.io/name: cloudlink
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # mongodb://mongodb-service:27017/url-shortener (base64)
  MONGO_URI: bW9uZ29kYjovL21vbmdvZGItc2VydmljZToyNzAxNy91cmwtc2hvcnRlbmVy
