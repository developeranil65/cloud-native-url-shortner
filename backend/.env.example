# =============================================================================
# Backend Environment Variables
# =============================================================================
# IMPORTANT: Do NOT use "localhost" in production or container deployments.
# Use Docker service names (e.g., "mongodb") or Kubernetes service names.
# =============================================================================

PORT=5000
MONGO_URI=mongodb://mongodb:27017/url-shortener
BASE_URL=http://backend:5000
NODE_ENV=production
